<Window x:Class="Hotel_Booking_System.Views.AddHotelDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Hotel"
        Height="650"
        Width="760"
        MinHeight="600"
        MinWidth="700"
        WindowStartupLocation="CenterOwner"
        Background="#FFF8F9FA">
    <Window.Resources>
        <x:Array x:Key="CityOptions" Type="sys:String" xmlns:sys="clr-namespace:System;assembly=mscorlib">
            <sys:String>Hà Nội</sys:String>
            <sys:String>Tuyên Quang</sys:String>
            <sys:String>Lào Cai</sys:String>
            <sys:String>Thái Nguyên</sys:String>
            <sys:String>Phú Thọ</sys:String>
            <sys:String>Bắc Ninh</sys:String>
            <sys:String>Hưng Yên</sys:String>
            <sys:String>Hải Phòng</sys:String>
            <sys:String>Ninh Bình</sys:String>
            <sys:String>Quảng Trị</sys:String>
            <sys:String>Đà Nẵng</sys:String>
            <sys:String>Quảng Ngãi</sys:String>
            <sys:String>Gia Lai</sys:String>
            <sys:String>Khánh Hòa</sys:String>
            <sys:String>Lâm Đồng</sys:String>
            <sys:String>Đăk Lăk</sys:String>
            <sys:String>TP. Hồ Chí Minh</sys:String>
            <sys:String>TP. Cần Thơ</sys:String>
            <sys:String>Vĩnh Long</sys:String>
            <sys:String>Đồng Tháp</sys:String>
            <sys:String>Cà Mau</sys:String>
            <sys:String>An Giang</sys:String>
        </x:Array>
    </Window.Resources>

    <Grid Margin="30">
        <Border Style="{StaticResource CardStyle}"
                CornerRadius="22"
                BorderThickness="0"
                Background="White"
                Padding="0"
                Margin="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="140" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Border Grid.Row="0"
                        Background="{StaticResource PrimaryGradient}"
                        CornerRadius="22,22,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border Width="72"
                                Height="72"
                                Margin="30"
                                CornerRadius="36"
                                Background="#FFFFFF22"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center">
                            <Viewbox Stretch="Uniform" Margin="12">
                                <TextBlock Text="🏨" />
                            </Viewbox>
                        </Border>
                        <StackPanel Grid.Column="1"
                                    Margin="0,30,30,30"
                                    VerticalAlignment="Center">
                            <TextBlock Text="Thêm khách sạn mới"
                                       FontSize="26"
                                       FontWeight="Bold"
                                       Foreground="White" />
                            <TextBlock Text="Điền đầy đủ thông tin bên dưới để khách sạn của bạn sẵn sàng hiển thị."
                                       Margin="0,10,0,0"
                                       Foreground="#F0F4FF" />
                        </StackPanel>
                    </Grid>
                </Border>

                <ScrollViewer Grid.Row="1"
                              Margin="30"
                              VerticalScrollBarVisibility="Auto">
                    <StackPanel Orientation="Vertical">
                        <Border Background="#F7F9FC"
                                CornerRadius="18"
                                Padding="24"
                                Margin="0,0,0,24">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="220" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0"
                                            HorizontalAlignment="Stretch">
                                    <Border Height="190"
                                            CornerRadius="18"
                                            Background="White"
                                            BorderBrush="#E2E8F0"
                                            BorderThickness="1"
                                            ClipToBounds="True">
                                        <Grid>
                                            <Image Source="{Binding HotelImage}"
                                                   Stretch="UniformToFill" />
                                            <TextBlock Text="Chưa có ảnh"
                                                       Foreground="#5B6C84"
                                                       FontWeight="SemiBold"
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding HotelImage}" Value="">
                                                                <Setter Property="Visibility" Value="Visible" />
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding HotelImage}" Value="{x:Null}">
                                                                <Setter Property="Visibility" Value="Visible" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Grid>
                                    </Border>
                                    <Button Content="Tải ảnh khách sạn"
                                            Style="{StaticResource PrimaryButton}"
                                            Height="48"
                                            Margin="0,18,0,0"
                                            CommandParameter="{Binding}"
                                            Click="UploadImage_Click" />
                                    <TextBlock Text="Hình ảnh sẽ giúp khách hàng tin tưởng và lựa chọn khách sạn của bạn."
                                               TextWrapping="Wrap"
                                               Foreground="#5B6C84"
                                               Margin="0,12,0,0" />
                                </StackPanel>

                                <StackPanel Grid.Column="1"
                                            Margin="30,0,0,0">
                                    <Grid Margin="0,8,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="Tên khách sạn"
                                                       FontWeight="SemiBold"
                                                       Foreground="#2C3E50"
                                                       Margin="0,0,0,8" />
                                            <TextBox Text="{Binding HotelName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                     Style="{StaticResource ModernTextBox}" />
                                        </StackPanel>
                                        <StackPanel Grid.Column="1"
                                                    Margin="20,0,0,0">
                                            <TextBlock Text="Thành phố"
                                                       FontWeight="SemiBold"
                                                       Foreground="#2C3E50"
                                                       Margin="0,0,0,8" />
                                            <ComboBox ItemsSource="{StaticResource CityOptions}"
                                                      SelectedItem="{Binding City, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                      Style="{StaticResource ModernComboBox}" />
                                        </StackPanel>
                                    </Grid>

                                    <StackPanel Margin="0,24,0,0">
                                        <TextBlock Text="Địa chỉ chi tiết"
                                                   FontWeight="SemiBold"
                                                   Foreground="#2C3E50"
                                                   Margin="0,0,0,8" />
                                        <TextBox Text="{Binding Address, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource ModernTextBox}" />
                                    </StackPanel>

                                    <Grid Margin="0,24,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="Giá thấp nhất (VNĐ)"
                                                       FontWeight="SemiBold"
                                                       Foreground="#2C3E50"
                                                       Margin="0,0,0,8" />
                                            <TextBox Text="{Binding MinPrice, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                     Style="{StaticResource ModernTextBox}"
                                                     HorizontalContentAlignment="Right" />
                                        </StackPanel>
                                        <StackPanel Grid.Column="1"
                                                    Margin="20,0,0,0">
                                            <TextBlock Text="Giá cao nhất (VNĐ)"
                                                       FontWeight="SemiBold"
                                                       Foreground="#2C3E50"
                                                       Margin="0,0,0,8" />
                                            <TextBox Text="{Binding MaxPrice, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                     Style="{StaticResource ModernTextBox}"
                                                     HorizontalContentAlignment="Right" />
                                        </StackPanel>
                                    </Grid>

                                    <Grid Margin="0,24,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="Xếp hạng"
                                                       FontWeight="SemiBold"
                                                       Foreground="#2C3E50"
                                                       Margin="0,0,0,8" />
                                            <ComboBox SelectedValuePath="Tag"
                                                      SelectedValue="{Binding Rating, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                      Style="{StaticResource ModernComboBox}">
                                                <ComboBoxItem Tag="5" Content="⭐⭐⭐⭐⭐ (5 sao)" />
                                                <ComboBoxItem Tag="4" Content="⭐⭐⭐⭐ (4 sao)" />
                                                <ComboBoxItem Tag="3" Content="⭐⭐⭐ (3 sao)" />
                                                <ComboBoxItem Tag="2" Content="⭐⭐ (2 sao)" />
                                                <ComboBoxItem Tag="1" Content="⭐ (1 sao)" />
                                            </ComboBox>
                                        </StackPanel>
                                        <StackPanel Grid.Column="1"
                                                    Margin="20,0,0,0">
                                            <TextBlock Text="Trạng thái hiển thị"
                                                       FontWeight="SemiBold"
                                                       Foreground="#2C3E50"
                                                       Margin="0,0,0,8" />
                                            <CheckBox Content="Hiển thị cho khách hàng"
                                                      Style="{StaticResource ModernCheckBox}"
                                                      IsChecked="{Binding IsVisible, Mode=TwoWay}" />
                                        </StackPanel>
                                    </Grid>

                                    <StackPanel Margin="0,24,0,0">
                                        <TextBlock Text="Mô tả chi tiết"
                                                   FontWeight="SemiBold"
                                                   Foreground="#2C3E50"
                                                   Margin="0,0,0,8" />
                                        <TextBox Text="{Binding Description, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource ModernTextBox}"
                                                 Height="140"
                                                 TextWrapping="Wrap"
                                                 AcceptsReturn="True"
                                                 VerticalScrollBarVisibility="Auto" />
                                    </StackPanel>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </StackPanel>
                </ScrollViewer>

                <Grid Grid.Row="2"
                      Margin="30,0,30,30">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal"
                                VerticalAlignment="Center"
                                Margin="0,0,16,0">
                        <Border Width="40"
                                Height="40"
                                Background="#EEF2FF"
                                CornerRadius="20"
                                Margin="0,0,12,0"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center">
                            <Viewbox Stretch="Uniform">
                                <TextBlock Text="ℹ"
                                           FontWeight="SemiBold"
                                           Foreground="#5B6C84"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center" />
                            </Viewbox>
                        </Border>
                        <TextBlock Text="Kiểm tra lại thông tin trước khi lưu."
                                   Foreground="#607089"
                                   VerticalAlignment="Center" />
                    </StackPanel>
                    <Button x:Name="btnCancel"
                            Grid.Column="1"
                            Content="Huỷ"
                            Width="140"
                            Margin="0,0,14,0"
                            Style="{StaticResource SecondaryButton}"
                            Click="Cancel_Click" />
                    <Button x:Name="btnSave"
                            Grid.Column="2"
                            Content="Lưu khách sạn"
                            Width="180"
                            Style="{StaticResource PrimaryButton}"
                            Click="Save_Click" />
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
