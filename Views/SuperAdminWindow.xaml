<Window x:Class="Hotel_Booking_System.Views.SuperAdminWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hotel_Booking_System.Views"
        mc:Ignorable="d"
        Title="Multi-Hotel Booking System" Height="900" Width="1400"
        WindowStartupLocation="CenterScreen" ResizeMode="CanResize">

    <Grid Background="#FFF8F9FA">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="#FF1976D2" Height="80">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="30,0">
                <TextBlock Text="" FontSize="32" VerticalAlignment="Center" Margin="0,0,15,0"/>
                <TextBlock Text="Hotel Booking System" 
                          FontSize="24" FontWeight="Bold" 
                          Foreground="White" 
                          VerticalAlignment="Center"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="30,0">
                <TextBlock Text="Xin Chào," Foreground="White" Style="{StaticResource HeaderTextStyle}" VerticalAlignment="Bottom"/>
                <TextBlock Text="{Binding CurrentUser.FullName}" Foreground="White" Style="{StaticResource HeaderTextStyle}" VerticalAlignment="Bottom"/>

                <Button Content="Logout" Style="{StaticResource PrimaryButton}" Width="100"
                       Background="#FFFF5722" Margin="5,0,0,0"/>
            </StackPanel>
        </Grid>

        <TabControl x:Name="MainTabControl" Background="White" BorderThickness="0" Margin="10,80,-10,32" Grid.RowSpan="2">



 
            <TabItem Style="{StaticResource TabItemStyle}" x:Name="SuperAdminDashboard" Header="👑 System Overview" Visibility="Visible">
                <ScrollViewer Margin="30,20" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="Super Admin Dashboard" Style="{StaticResource HeaderTextStyle}"/>

        
                        <Grid Margin="0,0,0,30">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" Style="{StaticResource CardStyle}" Background="#FF4CAF50">
                                <StackPanel Margin="15" HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding TotalHotels}" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                                    <TextBlock Text="Total Hotels" Foreground="White" HorizontalAlignment="Center" FontSize="12"/>
                                </StackPanel>
                            </Border>

                            <Border Grid.Column="1" Style="{StaticResource CardStyle}" Background="#FF2196F3">
                                <StackPanel Margin="15" HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding TotalUsers}" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                                    <TextBlock Text="Total Users" Foreground="White" HorizontalAlignment="Center" FontSize="12"/>
                                </StackPanel>
                            </Border>

                            <Border Grid.Column="2" Style="{StaticResource CardStyle}" Background="#FFFF9800">
                                <StackPanel Margin="15" HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding ActiveBookings}" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                                    <TextBlock Text="Active Bookings" Foreground="White" HorizontalAlignment="Center" FontSize="12"/>
                                </StackPanel>
                            </Border>

                            <Border Grid.Column="3" Style="{StaticResource CardStyle}" Background="#FFFF5722">
                                <StackPanel Margin="15" HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding MonthlyRevenue, StringFormat={}{0:C}}" FontSize="20" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                                    <TextBlock Text="Monthly Revenue" Foreground="White" HorizontalAlignment="Center" FontSize="12"/>
                                </StackPanel>
                            </Border>

                            <Border Grid.Column="4" Style="{StaticResource CardStyle}" Background="#FF9C27B0">
                                <StackPanel Margin="15" HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding PendingApprovals}" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                                    <TextBlock Text="Pending Approvals" Foreground="White" HorizontalAlignment="Center" FontSize="12"/>
                                </StackPanel>
                            </Border>
                        </Grid>

                        <Border Style="{StaticResource CardStyle}" Margin="0,0,0,20">
                            <StackPanel Margin="25">
                                <TextBlock Text="Pending Hotel Admin Requests" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

                                <DataGrid Height="200" AutoGenerateColumns="False" CanUserAddRows="False" ItemsSource="{Binding PendingRequest}">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="Request ID" Width="100" Binding="{Binding RequestID}"/>
                                        <DataGridTextColumn Header="Applicant Name" Width="150" Binding="{Binding ApplicantName}"/>
                                        <DataGridTextColumn Header="Hotel Name" Width="200" Binding="{Binding HotelName}"/>
                                        <DataGridTextColumn Header="Hotel Address" Width="200" Binding="{Binding HotelAddress}"/>
                                        <DataGridTextColumn Header="Date Applied" Width="150" Binding="{Binding CreatedAt, StringFormat={}{0:MMM dd, yyyy}}"/>
                                        <DataGridTemplateColumn Header="Actions" Width="180">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <StackPanel Orientation="Horizontal">
                                                        <Button Content="👁️ View" Style="{StaticResource SecondaryButton}"
                                                               Width="45" Height="25" Margin="2" FontSize="10"/>
                                                        <Button Content="✅ Approve" Style="{StaticResource SecondaryButton}"
                                                               Background="#FF4CAF50" Width="55" Height="25" Margin="2" FontSize="10"
                                                               Command="{Binding DataContext.ApproveRequestCommand, RelativeSource={RelativeSource AncestorType={x:Type Window}}}"
                                                               CommandParameter="{Binding RequestID}"/>
                                                        <Button Content="❌ Reject" Style="{StaticResource SecondaryButton}"
                                                               Background="#FFFF5722" Width="50" Height="25" Margin="2" FontSize="10"
                                                               Command="{Binding DataContext.RejectRequestCommand, RelativeSource={RelativeSource AncestorType={x:Type Window}}}"
                                                               CommandParameter="{Binding RequestID}"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource CardStyle}" Margin="0,0,0,20">
                            <StackPanel Margin="25">
                                <TextBlock Text="Pending Hotels" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>
                                <DataGrid Height="200" AutoGenerateColumns="False" CanUserAddRows="False" ItemsSource="{Binding PendingHotels}">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="Hotel ID" Width="100" Binding="{Binding HotelID}"/>
                                        <DataGridTextColumn Header="Hotel Name" Width="200" Binding="{Binding HotelName}"/>
                                        <DataGridTextColumn Header="City" Width="150" Binding="{Binding City}"/>
                                        <DataGridTemplateColumn Header="Actions" Width="150">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <StackPanel Orientation="Horizontal">
                                                        <Button Content="✅ Approve" Style="{StaticResource SecondaryButton}"
                                                                Background="#FF4CAF50" Width="55" Height="25" Margin="2" FontSize="10"
                                                                Command="{Binding DataContext.ApproveHotelCommand, RelativeSource={RelativeSource AncestorType={x:Type Window}}}"
                                                                CommandParameter="{Binding HotelID}"/>
                                                        <Button Content="❌ Reject" Style="{StaticResource SecondaryButton}"
                                                                Background="#FFFF5722" Width="50" Height="25" Margin="2" FontSize="10"
                                                                Command="{Binding DataContext.RejectHotelCommand, RelativeSource={RelativeSource AncestorType={x:Type Window}}}"
                                                                CommandParameter="{Binding HotelID}"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource CardStyle}">
                            <StackPanel Margin="25">
                                <TextBlock Text="System Reports and Analytics" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0" Margin="0,0,15,0">
                                        <Button Content="📊 Revenue Report" Style="{StaticResource PrimaryButton}" 
                                               HorizontalAlignment="Stretch" Margin="0,5"/>
                                        <Button Content="🏨 Hotel Performance" Style="{StaticResource PrimaryButton}" 
                                               HorizontalAlignment="Stretch" Margin="0,5"/>
                                        <Button Content="👥 User Analytics" Style="{StaticResource PrimaryButton}" 
                                               HorizontalAlignment="Stretch" Margin="0,5"/>
                                        <Button Content="📋 Booking Trends" Style="{StaticResource PrimaryButton}" 
                                               HorizontalAlignment="Stretch" Margin="0,5"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1" Margin="15,0,0,0">
                                        <Button Content="💰 Payment Summary" Style="{StaticResource PrimaryButton}" 
                                               HorizontalAlignment="Stretch" Margin="0,5"/>
                                        <Button Content="⭐ Review Analysis" Style="{StaticResource PrimaryButton}" 
                                               HorizontalAlignment="Stretch" Margin="0,5"/>
                                        <Button Content="🚨 System Issues" Style="{StaticResource PrimaryButton}" 
                                               Background="#FFFF5722" HorizontalAlignment="Stretch" Margin="0,5"/>
                                        <Button Content="📤 Export All Data" Style="{StaticResource PrimaryButton}" 
                                               Background="#FF4CAF50" HorizontalAlignment="Stretch" Margin="0,5"/>
                                    </StackPanel>
                                </Grid>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>


            <TabItem x:Name="HotelManagementTab" Header="🏨 Hotel Management" Visibility="Visible">
                <ScrollViewer Margin="30,20" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="Hotel Management" Style="{StaticResource HeaderTextStyle}"/>

                        <Grid Margin="0,0,0,20">
                            <StackPanel Orientation="Horizontal">
                                <Button Content="All Hotels" Style="{StaticResource PrimaryButton}" Width="100"/>
                                <Button Content="Active" Background="#FF4CAF50" Style="{StaticResource PrimaryButton}" Width="80"/>
                                <Button Content="Pending" Background="#FFFF9800" Style="{StaticResource PrimaryButton}" Width="80"/>
                                <Button Content="Suspended" Background="#FFFF5722" Style="{StaticResource PrimaryButton}" Width="100"/>
                                <ComboBox Style="{StaticResource ModernComboBox}" Width="120" Margin="10,0,0,0">
                                    <ComboBoxItem Content="All Cities"/>
                                    <ComboBoxItem Content="Hà Nội"/>
                                    <ComboBoxItem Content="TP. Hồ Chí Minh"/>
                                    <ComboBoxItem Content="Đà Nẵng"/>
                                    <ComboBoxItem Content="Hải Phòng"/>
                                </ComboBox>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <TextBox Text="Search hotels..." 
                            Style="{StaticResource ModernTextBox}" Width="200" Margin="0,0,10,0"/>
                                <Button Content="🔍 Search" Width="100" Style="{StaticResource SecondaryButton}"/>
                                <Button Content="+ Add Hotel" Width="120" Style="{StaticResource PrimaryButton}" 
                           Background="#FF4CAF50" Margin="10,0,0,0"/>
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0,0,0,30">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" Style="{StaticResource CardStyle}" Background="#FF4CAF50">
                                <StackPanel Margin="15" HorizontalAlignment="Center">
                                    <TextBlock Text="142" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                                    <TextBlock Text="Active Hotels" Foreground="White" HorizontalAlignment="Center" FontSize="12"/>
                                </StackPanel>
                            </Border>

                            <Border Grid.Column="1" Style="{StaticResource CardStyle}" Background="#FFFF9800">
                                <StackPanel Margin="15" HorizontalAlignment="Center">
                                    <TextBlock Text="14" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                                    <TextBlock Text="Pending Approval" Foreground="White" HorizontalAlignment="Center" FontSize="12"/>
                                </StackPanel>
                            </Border>

                            <Border Grid.Column="2" Style="{StaticResource CardStyle}" Background="#FFFF5722">
                                <StackPanel Margin="15" HorizontalAlignment="Center">
                                    <TextBlock Text="8" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                                    <TextBlock Text="Suspended" Foreground="White" HorizontalAlignment="Center" FontSize="12"/>
                                </StackPanel>
                            </Border>

                            <Border Grid.Column="3" Style="{StaticResource CardStyle}" Background="#FF2196F3">
                                <StackPanel Margin="15" HorizontalAlignment="Center">
                                    <TextBlock Text="4.2" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                                    <TextBlock Text="Avg Rating" Foreground="White" HorizontalAlignment="Center" FontSize="12"/>
                                </StackPanel>
                            </Border>
                        </Grid>

                        <Border Style="{StaticResource CardStyle}">
                            <StackPanel Margin="25">
                                <Grid Margin="0,0,0,15">
                                    <TextBlock Text="Hotel Directory" FontSize="18" FontWeight="Bold"/>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <Button Content="📊 Export" Style="{StaticResource SecondaryButton}" Width="80" Margin="0,0,5,0"/>
                                        <Button Content="🔄 Refresh" Style="{StaticResource SecondaryButton}" Width="80"/>
                                    </StackPanel>
                                </Grid>

                                <DataGrid Height="500" AutoGenerateColumns="False" CanUserAddRows="False">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="Hotel ID" Width="80" Binding="{Binding HotelId}"/>
                                        <DataGridTextColumn Header="Hotel Name" Width="200" Binding="{Binding HotelName}"/>
                                        <DataGridTextColumn Header="City" Width="100" Binding="{Binding City}"/>
                                        <DataGridTextColumn Header="Address" Width="250" Binding="{Binding Address}"/>
                                        <DataGridTextColumn Header="Star Rating" Width="80" Binding="{Binding StarRating}"/>
                                        <DataGridTextColumn Header="Total Rooms" Width="90" Binding="{Binding TotalRooms}"/>
                                        <DataGridTextColumn Header="Admin" Width="120" Binding="{Binding AdminName}"/>
                                        <DataGridTemplateColumn Header="Status" Width="80">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Border CornerRadius="12" Padding="8,4" HorizontalAlignment="Center">
                                                        <Border.Style>
                                                            <Style TargetType="Border">
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding Status}" Value="Active">
                                                                        <Setter Property="Background" Value="#FF4CAF50"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding Status}" Value="Pending">
                                                                        <Setter Property="Background" Value="#FFFF9800"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding Status}" Value="Suspended">
                                                                        <Setter Property="Background" Value="#FFFF5722"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Border.Style>
                                                        <TextBlock Text="{Binding Status}" Foreground="White" FontSize="10" FontWeight="Bold"/>
                                                    </Border>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                        <DataGridTextColumn Header="Created Date" Width="100" Binding="{Binding CreatedDate, StringFormat='{}{0:dd/MM/yyyy}'}"/>
                                        <DataGridTemplateColumn Header="Actions" Width="220">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <StackPanel Orientation="Horizontal">
                                                        <Button Content="👁️" ToolTip="View Details" Style="{StaticResource SecondaryButton}" 
                                                   Width="25" Height="25" Margin="2" FontSize="10"/>
                                                        <Button Content="✏️" ToolTip="Edit Hotel" Style="{StaticResource SecondaryButton}" 
                                                   Background="#FF2196F3" Width="25" Height="25" Margin="2" FontSize="10"/>
                                                        <Button Content="🏠" ToolTip="Manage Rooms" Style="{StaticResource SecondaryButton}" 
                                                   Background="#FFFF9800" Width="25" Height="25" Margin="2" FontSize="10"/>
                                                        <Button Content="📊" ToolTip="Analytics" Style="{StaticResource SecondaryButton}" 
                                                   Background="#FF9C27B0" Width="25" Height="25" Margin="2" FontSize="10"/>
                                                        <Button Content="⚠️" ToolTip="Suspend" Style="{StaticResource SecondaryButton}" 
                                                   Background="#FFFF5722" Width="25" Height="25" Margin="2" FontSize="10"/>
                                                        <Button Content="📧" ToolTip="Contact Admin" Style="{StaticResource SecondaryButton}" 
                                                   Background="#FF607D8B" Width="25" Height="25" Margin="2" FontSize="10"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                    </DataGrid.Columns>
                                </DataGrid>

                                <Grid Margin="0,15,0,0">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="Showing 1-10 of 156 hotels" VerticalAlignment="Center" FontSize="12" Foreground="#FF666666"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <Button Content="Previous" Style="{StaticResource SecondaryButton}" Width="70" Height="30" Margin="0,0,5,0"/>
                                        <Button Content="1" Style="{StaticResource PrimaryButton}" Width="30" Height="30" Margin="2"/>
                                        <Button Content="2" Style="{StaticResource SecondaryButton}" Width="30" Height="30" Margin="2"/>
                                        <Button Content="3" Style="{StaticResource SecondaryButton}" Width="30" Height="30" Margin="2"/>
                                        <Button Content="..." Style="{StaticResource SecondaryButton}" Width="30" Height="30" Margin="2"/>
                                        <Button Content="16" Style="{StaticResource SecondaryButton}" Width="30" Height="30" Margin="2"/>
                                        <Button Content="Next" Style="{StaticResource SecondaryButton}" Width="70" Height="30" Margin="5,0,0,0"/>
                                    </StackPanel>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource CardStyle}" Margin="0,20,0,0">
                            <StackPanel Margin="25">
                                <TextBlock Text="Quick Actions Reports" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0" Margin="0,0,15,0">
                                        <Button Content="📈 Hotel Performance Report" Style="{StaticResource PrimaryButton}" 
                                   HorizontalAlignment="Stretch" Margin="0,5"/>
                                        <Button Content="🏆 Top Performing Hotels" Style="{StaticResource PrimaryButton}" 
                                   HorizontalAlignment="Stretch" Margin="0,5"/>
                                        <Button Content="📋 Occupancy Analytics" Style="{StaticResource PrimaryButton}" 
                                   HorizontalAlignment="Stretch" Margin="0,5"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1" Margin="7.5,0">
                                        <Button Content="⭐ Rating Review Summary" Style="{StaticResource PrimaryButton}" 
                                   HorizontalAlignment="Stretch" Margin="0,5"/>
                                        <Button Content="💰 Revenue by Hotel" Style="{StaticResource PrimaryButton}" 
                                   HorizontalAlignment="Stretch" Margin="0,5"/>
                                        <Button Content="🚨 Hotels Needing Attention" Style="{StaticResource PrimaryButton}" 
                                   Background="#FFFF5722" HorizontalAlignment="Stretch" Margin="0,5"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="2" Margin="15,0,0,0">
                                        <Button Content="📊 Bulk Update Hotels" Style="{StaticResource PrimaryButton}" 
                                   Background="#FFFF9800" HorizontalAlignment="Stretch" Margin="0,5"/>
                                        <Button Content="📤 Export Hotel Data" Style="{StaticResource PrimaryButton}" 
                                   Background="#FF4CAF50" HorizontalAlignment="Stretch" Margin="0,5"/>
                                        <Button Content="🔧 System Maintenance" Style="{StaticResource PrimaryButton}" 
                                   Background="#FF9C27B0" HorizontalAlignment="Stretch" Margin="0,5"/>
                                    </StackPanel>
                                </Grid>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <TabItem x:Name="UserManagementTab" Header="👥 User Management" Visibility="Visible">
                <ScrollViewer Margin="30,20" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="User Management" Style="{StaticResource HeaderTextStyle}"/>

                        <Grid Margin="0,0,0,20">
                            <StackPanel Orientation="Horizontal">
                                <Button Content="All Users" Style="{StaticResource PrimaryButton}" Width="100"/>
                                <Button Content="Customers" Background="#FF2196F3" Style="{StaticResource PrimaryButton}" Width="100"/>
                                <Button Content="Hotel Admins" Background="#FFFF9800" Style="{StaticResource PrimaryButton}" Width="120"/>
                                <Button Content="Super Admins" Background="#FFFF5722" Style="{StaticResource PrimaryButton}" Width="120"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">

                                <TextBox Text="Search users..." 
                                        Style="{StaticResource ModernTextBox}" Margin="0,0,10,0"/>
                                <Button Content="🔍 Search" Width="100" Style="{StaticResource SecondaryButton}"/>
                            </StackPanel>
                        </Grid>

                        <Border Style="{StaticResource CardStyle}">
                            <StackPanel Margin="25">
                                <DataGrid Height="400" AutoGenerateColumns="False" CanUserAddRows="False">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="User ID" Width="80" Binding="{Binding UserId}"/>
                                        <DataGridTextColumn Header="Name" Width="150" Binding="{Binding FullName}"/>
                                        <DataGridTextColumn Header="Email" Width="200" Binding="{Binding Email}"/>
                                        <DataGridTextColumn Header="Role" Width="100" Binding="{Binding Role}"/>
                                        <DataGridTextColumn Header="Status" Width="80" Binding="{Binding Status}"/>
                                        <DataGridTextColumn Header="Last Login" Width="100" Binding="{Binding LastLogin}"/>
                                        <DataGridTemplateColumn Header="Actions" Width="200">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <StackPanel Orientation="Horizontal">
                                                        <Button Content="👁️" Style="{StaticResource SecondaryButton}" 
                                                               Width="25" Height="25" Margin="2" FontSize="10"/>
                                                        <Button Content="✏️" Style="{StaticResource SecondaryButton}" 
                                                               Background="#FFFF9800" Width="25" Height="25" Margin="2" FontSize="10"/>
                                                        <Button Content="🚫" Style="{StaticResource SecondaryButton}" 
                                                               Background="#FFFF5722" Width="25" Height="25" Margin="2" FontSize="10"/>
                                                        <Button Content="📧" Style="{StaticResource SecondaryButton}" 
                                                               Background="#FF2196F3" Width="25" Height="25" Margin="2" FontSize="10"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>


            <TabItem Style="{StaticResource TabItemStyle}" Header="👤 Profile" Visibility="Visible">
                <ScrollViewer Margin="30,20" VerticalScrollBarVisibility="Auto">
                    <Grid MaxWidth="1000">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="300"/>
                        </Grid.ColumnDefinitions>


                        <StackPanel Grid.Column="0" Margin="0,0,30,0">


                            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,20">
                                <StackPanel Margin="25">
                                    <TextBlock Text="Personal Information" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>


                                        <StackPanel Grid.Row="0" Grid.ColumnSpan="2" Margin="0,0,0,15">
                                            <TextBlock Text="User ID" FontWeight="SemiBold" Margin="0,0,0,5"/>
                                            <TextBox Text="{Binding CurrentUser.UserID}" 
                                         Style="{StaticResource ModernTextBox}"
                                         IsReadOnly="True"
                                                     BorderThickness="0"
                                         Background="#FFF5F5F5"
                                         Foreground="#FF666666"/>
                                        </StackPanel>

                                        <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Margin="0,0,0,15">
                                            <TextBlock Text="Full Name" FontWeight="SemiBold" Margin="0,0,0,5"/>
                                            <TextBox Text="{Binding CurrentUser.FullName}" Style="{StaticResource ModernTextBox}"/>
                                        </StackPanel>


                                        <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Margin="0,0,0,15">
                                            <TextBlock Text="Email Address" FontWeight="SemiBold" Margin="0,0,0,5"/>
                                            <TextBox IsReadOnly="True" Text="{Binding CurrentUser.Email}" Style="{StaticResource ModernTextBox}"/>
                                        </StackPanel>

                                        <StackPanel Grid.Row="3" Grid.Column="0" Margin="0,0,10,15">
                                            <TextBlock Text="Phone Number" FontWeight="SemiBold" Margin="0,0,0,5"/>
                                            <TextBox Text="{Binding CurrentUser.Phone}" Style="{StaticResource ModernTextBox}"/>
                                        </StackPanel>


                                        <StackPanel Grid.Row="3" Grid.Column="1" Margin="10,0,0,15">
                                            <TextBlock Text="Gender" FontWeight="SemiBold" Margin="0,0,0,5"/>
                                            <ComboBox SelectedValue="{Binding CurrentUser.Gender, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
          SelectedValuePath="Content" Style="{StaticResource ModernComboBox}">
                                                <ComboBoxItem Content="Male" />
                                                <ComboBoxItem Content="Female" />
                                                <ComboBoxItem Content="Other" />

                                            </ComboBox>
                                        </StackPanel>


                                        <StackPanel Grid.Row="4" Grid.Column="0" Margin="0,0,10,15">
                                            <TextBlock Text="Date of Birth" FontWeight="SemiBold" Margin="0,0,0,5"/>
                                            <DatePicker SelectedDate="{Binding CurrentUser.DateOfBirth}" Style="{StaticResource ModernDatePicker}"/>
                                        </StackPanel>


                                        <StackPanel Grid.Row="4" Grid.Column="1" Margin="10,0,0,15">
                                            <TextBlock Text="Role" FontWeight="SemiBold" Margin="0,0,0,5"/>
                                            <TextBox Text="{Binding CurrentUser.Role}" 
                                         Style="{StaticResource ModernTextBox}"
                                         IsReadOnly="True"
                                         Background="#FFF5F5F5"
                                         Foreground="#FF666666"/>
                                        </StackPanel>
                                    </Grid>

                                    <StackPanel Orientation="Horizontal" Margin="0,15,0,0">
                                        <Button Command="{Binding UpdateInfoCommand}" Content="Update Profile" Width="150" Style="{StaticResource PrimaryButton}" Margin="0,0,10,0"/>
                                        <Button Command="{Binding RegisterCommand}" Content="Register Hotel Admin" Background="#FFFF9800" 
                                    Style="{StaticResource PrimaryButton}" Width="200"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>


                            <Border Style="{StaticResource CardStyle}">
                                <StackPanel Margin="25">
                                    <TextBlock Text="Change Password" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>


                                        <StackPanel Grid.Row="0" Grid.ColumnSpan="2" Margin="0,0,0,15">
                                            <TextBlock Text="Current Password" FontWeight="SemiBold" Margin="0,0,0,5"/>
                                            <PasswordBox x:Name="txtCurrentPassword" 
                                           Style="{StaticResource ModernPasswordBox}"
                                           />
                                        </StackPanel>

                                        <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,10,15">
                                            <TextBlock Text="New Password" FontWeight="SemiBold" Margin="0,0,0,5"/>
                                            <PasswordBox x:Name="txtNewPassword" 
                                           Style="{StaticResource ModernPasswordBox}"
                                           />
                                        </StackPanel>

                                        <StackPanel Grid.Row="1" Grid.Column="1" Margin="10,0,0,15">
                                            <TextBlock Text="Confirm New Password" FontWeight="SemiBold" Margin="0,0,0,5"/>
                                            <PasswordBox x:Name="txtConfirmPassword" 
                                           Style="{StaticResource ModernPasswordBox}"
                                           />
                                        </StackPanel>


                                        <Border Grid.Row="2" Grid.ColumnSpan="2" Background="#FFF3E5F5" CornerRadius="8" Padding="12" Margin="0,0,0,15">
                                            <StackPanel>
                                                <TextBlock Text="Password Requirements:" FontWeight="SemiBold" FontSize="12" Margin="0,0,0,5"/>
                                                <TextBlock Text="• At least 8 characters long" FontSize="11" Margin="0,2"/>
                                                <TextBlock Text="• Contains uppercase and lowercase letters" FontSize="11" Margin="0,2"/>
                                                <TextBlock Text="• Contains at least one number" FontSize="11" Margin="0,2"/>
                                                <TextBlock Text="• Contains at least one special character" FontSize="11" Margin="0,2"/>
                                            </StackPanel>
                                        </Border>
                                    </Grid>

                                    <StackPanel Orientation="Horizontal" Margin="0,15,0,0">
                                        <Button Content="Change Password" Width="150" Style="{StaticResource PrimaryButton}" 
                                    Background="#FFFF5722" Margin="0,0,10,0"/>
                                        <Button Content="Cancel" Width="100" Style="{StaticResource SecondaryButton}"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </StackPanel>


                        <StackPanel Grid.Column="1">
                            <Border Style="{StaticResource CardStyle}">
                                <StackPanel Margin="25" HorizontalAlignment="Center">

                                    <Grid Width="100" Height="100">
                                        <Image Source="{Binding CurrentUser.AvatarUrl, FallbackValue=\Resources\avatar_default.png}"
           Stretch="UniformToFill">
                                            <Image.Clip>
                                                <EllipseGeometry Center="50,50" RadiusX="50" RadiusY="50"/>
                                            </Image.Clip>
                                        </Image>
                                        <Ellipse Stroke="#FFE0E0E0" StrokeThickness="3"/>
                                    </Grid>


                                    <TextBlock Text="{Binding CurrentUser.FullName}" FontSize="18" FontWeight="Bold" 
                                   HorizontalAlignment="Center" Margin="0,0,0,5"/>
                                    <Border 
                                CornerRadius="12" Padding="8,4" HorizontalAlignment="Center" Margin="0,0,0,20">
                                        <TextBlock Text="{Binding CurrentUser.Role}" FontSize="12" Foreground="White" 
                                       FontWeight="SemiBold" HorizontalAlignment="Center"/>
                                    </Border>


                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <StackPanel Grid.Row="0" Grid.Column="0" HorizontalAlignment="Center" Margin="0,5">
                                            <TextBlock Text="{Binding TotalBookings, FallbackValue=0}" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center"/>
                                            <TextBlock Text="Bookings" FontSize="10" Foreground="#FF666666" HorizontalAlignment="Center"/>
                                        </StackPanel>


                                        <StackPanel Grid.Row="0" Grid.Column="1" HorizontalAlignment="Center" Margin="0,5">
                                            <TextBlock Text="{Binding TotalSpent, StringFormat='${0:F0}',FallbackValue=0}" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center"/>
                                            <TextBlock Text="Spent" FontSize="10" Foreground="#FF666666" HorizontalAlignment="Center"/>
                                        </StackPanel>


                                        <Border Grid.Row="2" Grid.ColumnSpan="2" Background="#FFFFF3E0" CornerRadius="8" 
                                    Padding="10,8" Margin="0,15,0,0" HorizontalAlignment="Stretch">
                                            <StackPanel HorizontalAlignment="Center">
                                                <TextBlock Text="🏆 Membership Level" FontSize="10" Foreground="#FF666666" 
                                               HorizontalAlignment="Center"/>
                                                <TextBlock Text="{Binding MembershipLevel}" FontSize="14" FontWeight="Bold" 
                                               Foreground="#FFFF9800" HorizontalAlignment="Center"/>
                                            </StackPanel>
                                        </Border>
                                    </Grid>

                                    <StackPanel Margin="0,20,0,0">
                                        <Button Content="Upload Avatar" Width="150" Height="35"
                                                Command="{Binding UploadImageCommand}"
                                    Style="{StaticResource SecondaryButton}" Margin="0,0,0,8"/>

                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </StackPanel>



                    </Grid>



                </ScrollViewer>

            </TabItem>


            <TabItem x:Name="PaymentSummaryTab" Header="💰 Payment Summary">
                <ScrollViewer Margin="30,20" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="Payment Summary" Style="{StaticResource HeaderTextStyle}"/>
                        <DataGrid ItemsSource="{Binding Payments}" AutoGenerateColumns="False" CanUserAddRows="False" Margin="0,20,0,20">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Payment ID" Binding="{Binding PaymentID}" Width="*"/>
                                <DataGridTextColumn Header="Booking ID" Binding="{Binding BookingID}" Width="*"/>
                                <DataGridTextColumn Header="Amount" Binding="{Binding TotalPayment}" Width="*"/>
                                <DataGridTextColumn Header="Method" Binding="{Binding Method}" Width="*"/>
                                <DataGridTextColumn Header="Date" Binding="{Binding PaymentDate}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                        <TextBlock Text="{Binding TotalRevenue, StringFormat='Total Revenue: ${0:F0}'}" FontSize="16" FontWeight="Bold" Foreground="#27AE60" HorizontalAlignment="Right"/>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>


        </TabControl>
    </Grid>
</Window>
